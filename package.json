{
  "name": "marker-helper-dev",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "description": "開発・テスト専用のメタデータ。拡張機能本体はビルドレスの素のJSで、この package.json は出荷物に影響しない（テストツールのためだけに存在する）。",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
