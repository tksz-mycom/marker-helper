# Marker:HELPER

Webページ上の要素を**ホバーで強調・クリックで固定マーク**し、サイドパネルでマーク一覧とCSSセレクタを確認・コピーできる Chrome 拡張機能です（Manifest V3）。

UIテストのセレクタ収集、デザインレビューの指摘箇所メモ、スクレイピング対象の特定などに使えます。

## 機能

| 機能 | 説明 |
|------|------|
| ホバーハイライト | マウス位置の要素を破線枠でリアルタイム強調 |
| クリックでマーク固定 | 要素に番号付きの枠を付与（同じ要素を再クリックで解除） |
| 色・線種・線幅・余白・角丸の選択 | ポップアップでプリセット色（白を含む）／カスタム色・実線/破線/点線・線幅・余白（枠と要素のすき間）・角丸を変更。余白と角丸はスライダー＋数値入力で 4px 刻み（0〜40px） |
| マーク一覧（サイドパネル） | マークした要素のタグ・CSSセレクタ・テキストを一覧表示 |
| セレクタコピー | 各行から個別コピー、または「まとめてコピー」で全件を改行区切りで取得 |
| 位置へ移動 | 一覧から該当要素へスクロール＆点滅 |
| マーク部分のスクリーンショット | サイドパネルの各マーク項目から「画像保存」「画像コピー」でその要素部分だけを PNG 取得。保存／コピーの両対応、枠・番号の有無をヘッダーのトグルで選択可（`activeTab` 権限を使用） |

> マーク状態はセッション内のみ保持します（ページのリロードやタブを閉じると消えます）。

## インストール（開発者モード）

1. Chrome で `chrome://extensions` を開く
2. 右上の「**デベロッパー モード**」をオンにする
3. 「**パッケージ化されていない拡張機能を読み込む**」をクリック
4. このフォルダ（`manifest.json` がある階層）を選択

## 使い方

1. ツールバーの **Marker:HELPER** アイコンをクリックしてポップアップを開く
2. 「**マークモード**」をオンにする
3. 枠の色・線種・線幅・余白・角丸を選ぶ（連番ラベルは既定オフ。「表示」で切り替え）
4. ページ上の要素にマウスを乗せると強調表示され、クリックでマークが固定される
5. ポップアップの「**一覧パネルを開く**」でサイドパネルを表示し、セレクタを確認・コピーする

## ファイル構成

```
.
├── manifest.json        # 拡張機能の定義（MV3）
├── background.js        # service worker（サイドパネル挙動の設定）
├── content/
│   ├── content.js       # ホバー・クリック・ハイライト・マーク管理
│   └── content.css      # オーバーレイ・枠・番号バッジのスタイル
├── popup/
│   ├── popup.html       # 色・線種選択UI
│   ├── popup.css
│   └── popup.js
├── panel/
│   ├── panel.html       # マーク一覧サイドパネル
│   ├── panel.css
│   └── panel.js
└── icons/
    ├── icon.svg         # アイコン元データ
    ├── icon16.png
    ├── icon48.png
    └── icon128.png
```

## 仕組み（概要）

- **content script** がマーク状態の唯一の保持者（source of truth）。オーバーレイ層（`#mm-overlay-root`、`pointer-events: none`）に枠とバッジを描画し、`requestAnimationFrame` でスクロール・リサイズに追従させます。ページのDOM自体は書き換えません。
- **popup** はスタイル変更とマークモードのON/OFFを `chrome.tabs.sendMessage` で content に伝え、`chrome.sidePanel.open()` でサイドパネルを開きます。
- **panel** はアクティブタブの content に状態を問い合わせて一覧を描画し、content からの `MM_MARKS_UPDATED` 通知で同期します。

### メッセージ一覧

| type | 方向 | 内容 |
|------|------|------|
| `MM_GET_STATE` | popup/panel → content | 現在の `enabled` / `style` / `marks` を取得 |
| `MM_SET_ENABLED` | popup → content | マークモードのON/OFF |
| `MM_SET_STYLE` | popup → content | 新規マークに使う色・線種・線幅 |
| `MM_CLEAR_ALL` | popup/panel → content | 全マーク削除 |
| `MM_REMOVE_MARK` | panel → content | 指定IDのマーク削除 |
| `MM_SCROLL_TO` | panel → content | 指定IDの要素へスクロール |
| `MM_MARKS_UPDATED` | content → broadcast | マーク更新の通知（popup/panelが受信） |

## 制限事項

- `chrome://` などのブラウザ内部ページ、Chrome ウェブストア、`file://`（ファイルURLへのアクセス許可が無効の場合）では動作しません。
- マークはセレクタではなく要素参照で保持するため、SPAの再描画などで対象要素が消えると一覧に「消失」と表示されます。
- セレクタは `id`（一意な場合）優先、無ければ `nth-of-type` のパスで生成します。動的に変わるページでは安定しないことがあります。
- スクリーンショット機能では、縦長要素はビューポートを超える分が見切れます（スクロール継ぎ合わせは未実装）。
